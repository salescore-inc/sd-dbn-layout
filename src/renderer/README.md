# Renderer

The renderer draws positioned graph data in the browser.

The first renderer target should be SVG because it is inspectable and supports hover and selection affordances with simple DOM behavior.
